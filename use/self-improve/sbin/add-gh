#!/bin/sh
# add-gh: install GitHub CLI
set -e

apk add --no-cache curl bash
curl -fsSL https://github.com/cli/cli/releases/download/v2.63.0/gh_2.63.0_linux_amd64.tar.gz \
  | tar -xz -C /usr/local
ln -sf /usr/local/bin/gh /usr/local/bin/gh || true