# Podclaws root compose — goclaw service is defined in goclaw-build/compose.yml
# This file provides the shared network and sensible-tasks volume.
# Use overrides from goclaw-build/ for binary or dockerfile deployment.

networks:
  net:
    driver: bridge

volumes:
  sensible-tasks: