# Podclaws root compose — goclaw service is defined in use/goclaw/service.goclaw.yml
# This file provides the shared network and sensible-tasks volume.
# Use overrides from use/goclaw/ for binary or dockerfile deployment.

networks:
  net:
    driver: bridge
  goclaw-net:
    driver: bridge

volumes:
  sensible-tasks: